{
  "name": "dicey",
  "description": "Pen-and-paper roleplaying game aid - dice roller.",
  "version": "3.1.3",
  "scripts": {
    "start": "npm prune && npm install && npm run server",
    "server": "./node_modules/.bin/ws --key ws.key --cert ws.crt --verbose --compress --no-cache -d public/",
    "watch": "node ./scripts/watch.js",
    "ncu": "ncu --packageFile package.json --loglevel verbose"
  },
  "devDependencies": {
    "babel-preset-react": "^6.5.0",
    "babelify": "^7.2.0",
    "browserify": "^13.0.0",
    "droll": "^0.1.2",
    "firebase": "^2.4.2",
    "local-web-server": "^1.2.2",
    "react": "*",
    "react-dom": "^15.0.1",
    "sw-precache": "^3.1.1",
    "watchify": "^3.7.0"
  }
}
